<div dir="rtl" lang="he">

# תרגילים - שיעור 5.5: Python בסיסי לאינטגרציות ואוטומציות

## מידע כללי
- **שיעור:** 5.5 - Python בסיסי לאינטגרציות ואוטומציות
- **סוג השיעור:** מעשי (4 שעות)
- **הערה:** כל התרגילים מחייבים Python מותקן, סביבה וירטואלית (venv) פעילה, ומפתח Claude API פעיל (ניתן לקבל ב-console.anthropic.com)

---

## תרגיל 1: הקמת סביבת עבודה (מעשי - 15 דקות)
### הנחיות:
מטרת תרגיל זה היא לוודא שסביבת הפיתוח שלכם מוכנה ומאורגנת כראוי לעבודה עם אינטגרציות. סביבת עבודה נקייה ומבודדת היא קריטית בפרויקטים אמיתיים.

1.  פתחו טרמינל חדש בתיקיית הפרויקט.
2.  צרו סביבה וירטואלית חדשה בשם `venv`:
    ```bash
    python -m venv venv
    ```
3.  הפעילו את הסביבה הוירטואלית:
    -   **Windows:** `.\venv\Scripts\activate`
    -   **Mac/Linux:** `source venv/bin/activate`
    (לאחר ההפעלה, שם הסביבה `(venv)` יופיע בתחילת שורת הפקודה).
4.  צרו קובץ בשם `requirements.txt` והדביקו לתוכו את התוכן הבא:
    ```
    anthropic
    python-dotenv
    requests
    ```
5.  התקינו את הספריות מהקובץ שיצרתם באמצעות `pip`:
    ```bash
    pip install -r requirements.txt
    ```
6.  צרו קובץ בשם `.env` (שימו לב לנקודה בהתחלה). קובץ זה ישמש לאחסון משתני סביבה רגישים כמו מפתחות API.
7.  פתחו את קובץ ה-`.env` והוסיפו את מפתח ה-API שלכם מ-Anthropic (Claude) בפורמט הבא:
    ```
    ANTHROPIC_API_KEY="sk-ant-..."
    ```
    *החליפו את `"sk-ant-..."` במפתח האמיתי שלכם.*

### שאלות:
1.  מדוע חשוב להשתמש בסביבה וירטואלית (`venv`) בפרויקטי Python?
2.  מהו התפקיד של הקובץ `requirements.txt`?
3.  מדוע אין להעלות את קובץ ה-`.env` למערכות ניהול גרסאות כמו Git? (רמז: חשבו על אבטחת מידע).

---

## תרגיל 2: ניהול לידים בסיסי (כתיבת קוד - 20 דקות)
### הנחיות:
תרגול מבני נתונים בסיסיים של Python דרך משימה עסקית נפוצה: ניהול רשימת לידים.

1. צרו קובץ Python חדש בשם `manage_leads.py`.
2. העתיקו את קוד המוצא לקובץ. קוד זה מכיל רשימה של לידים, כאשר כל ליד הוא מילון (dictionary).
3. כתבו לולאת `for` שעוברת על כל הלידים ברשימה `leads`.
4. בתוך הלולאה, הדפיסו עבור כל ליד סיכום בפורמט הבא:
   `שם: [שם], חברה: [חברה], תפקיד: [תפקיד]`
5. הוסיפו תנאי `if` בתוך הלולאה שיבדוק אם התפקיד של הליד (`title`) מכיל את המילה "מנהל" או "Manager". אם כן, הדפיסו שורה נוספת: `>> ליד בעדיפות גבוהה`.

### קוד מוצא:
```python
# manage_leads.py

leads = [
    {
        "name": "דנה לוי",
        "company": "בנק לאומי",
        "title": "מנהלת שיווק דיגיטלי"
    },
    {
        "name": "יוסי כהן",
        "company": "אל על",
        "title": "Data Analyst"
    },
    {
        "name": "אביגיל שרון",
        "company": "Wix",
        "title": "Senior Product Manager"
    },
    {
        "name": "משה פרץ",
        "company": "תנובה",
        "title": "סמנכ\"ל תפעול"
    }
]

# כתבו את הקוד שלכם כאן
```

---

## תרגיל 3: קריאת API חיצונית ראשונה (אינטגרציה - 25 דקות)
### הנחיות:
נבצע קריאת `GET` ל-API ציבורי כדי לקבל נתונים עדכניים. נשתמש ב-API של בנק ישראל לקבלת שערי מטבע. זוהי דוגמה מצוינת לאינטגרציה עם שירותים חיצוניים.

1.  צרו קובץ חדש בשם `currency_converter.py`.
2.  השתמשו בספריית `requests` כדי לשלוח בקשת `GET` לכתובת ה-API של בנק ישראל לקבלת שער הדולר. הכתובת היא: `https://www.boi.org.il/boi_files/BoiRuledController.svc/GetSpecificCurrencyRate?currencyCode=USD`
3.  בדקו את ה-`status_code` של התשובה. אם הוא `200`, זה אומר שהבקשה הצליחה.
4.  המירו את התוכן שהתקבל (שהוא בפורמט JSON) לאובייקט Python באמצעות `response.json()`.
5.  חלצו את שער החליפין (`rate`) מתוך הנתונים.
6.  כתבו פונקציה `convert_shekel_to_usd(amount_nis)` שמקבלת סכום בשקלים ומחזירה את הסכום בדולרים על בסיס השער שקיבלתם.
7.  הדפיסו את התוצאה של המרת 15,000 ש"ח לדולר.

### קוד מוצא:
```python
# currency_converter.py
import requests

def get_usd_exchange_rate():
    """
    Fetches the current ILS to USD exchange rate from the Bank of Israel API.
    Returns the rate as a float, or None if the request fails.
    """
    url = "https://www.boi.org.il/boi_files/BoiRuledController.svc/GetSpecificCurrencyRate?currencyCode=USD"
    # כתבו את הקוד לקריאת ה-API כאן
    # ...
    # החזירו את השער
    pass

def convert_shekel_to_usd(amount_nis, rate):
    """Converts an amount from NIS to USD using a given rate."""
    # כתבו את קוד ההמרה כאן
    pass

# --- Main script ---
current_rate = get_usd_exchange_rate()
if current_rate:
    amount_in_nis = 15000
    amount_in_usd = convert_shekel_to_usd(amount_in_nis, current_rate)
    print(f"{amount_in_nis} ש\"ח שווים ל-${amount_in_usd:.2f}")
else:
    print("Failed to get the exchange rate.")

```

---

## תרגיל 4: אינטגרציה ראשונה עם Claude - העשרת ליד (אינטגרציה - 30 דקות)
### הנחיות:
זהו התרגיל הראשון בו נתממשק ל-AI. ניקח פרטי ליד בסיסיים ונשתמש ב-Claude כדי ליצור "שובר קרח" מותאם אישית לפתיחת שיחה עסקית.

1.  צרו קובץ חדש בשם `enrich_lead.py`.
2.  הגדירו את הלקוח של Claude API. ודאו שאתם טוענים את מפתח ה-API מקובץ ה-`.env` באמצעות `python-dotenv`.
3.  הגדירו משתנים עבור ליד ספציפי (לדוגמה, שם: "רוני מאור", חברה: "סאפיינס", תפקיד: "מנהלת פרויקטים בכירה").
4.  בנו פרומפט (prompt) ל-Claude. הפרומפט צריך לבקש ממנו לכתוב משפט פתיחה קצר (עד 20 מילים) למייל עבור הליד, בהתבסס על החברה והתפקיד שלו. הדגישו שהטון צריך להיות מקצועי וישראלי.
5.  בצעו קריאת API ל-Claude עם הפרומפט שיצרתם.
6.  הדפיסו את התשובה שקיבלתם מ-Claude.

### קוד מוצא:
```python
# enrich_lead.py
import os
import anthropic
from dotenv import load_dotenv

# Load environment variables from .env file
load_dotenv()

# --- Lead Information ---
lead_name = "רוני מאור"
lead_company = "סאפיינס טכנולוגיות"
lead_title = "מנהלת פרויקטים בכירה"

# 1. Initialize the Anthropic client
try:
    client = anthropic.Anthropic(
        # This will use the ANTHROPIC_API_KEY from your .env file
        api_key=os.environ.get("ANTHROPIC_API_KEY"),
    )
except Exception as e:
    print(f"Error initializing client: {e}")
    exit()

# 2. Create the prompt for Claude
prompt = f"""
היי קלוד,
אני צריך לכתוב מייל קר לליד בשם {lead_name}, שעובד/ת כ-{lead_title} בחברת {lead_company}.
אנא נסח עבורי משפט פתיחה קצר (עד 20 מילים) למייל, שיהיה מקצועי, מותאם אישית לתפקיד ולחברה, ובטון ישראלי.
"""

# 3. Call the Claude API
print("מייצר משפט פתיחה עם Claude...")

# כתבו כאן את הקוד לקריאת ה-API של Claude
# ...

# 4. Print the result
# print(f"הצעה למשפט פתיחה:\n{...}")
```

---

## תרגיל 5: איתור וטיפול בשגיאות (דיבאגינג - 25 דקות)
### הנחיות:
בעולם האמיתי, נתונים לא תמיד מגיעים נקיים. בתרגיל זה נלמד כיצד להתמודד עם שגיאות בנתונים באמצעות `try/except` מבלי שהתוכנית כולה תקרוס.

1. צרו קובץ בשם `leads.json` והדביקו לתוכו את התוכן המצורף. שימו לב שלאחד הלידים חסר המפתח "company".
2. צרו קובץ פייתון `process_leads_safely.py`.
3. כתבו קוד שקורא את הנתונים מהקובץ `leads.json`.
4. כתבו לולאה שעוברת על כל הלידים.
5. בתוך הלולאה, נסו להדפיס את השם והחברה של כל ליד.
6. עטפו את קוד ההדפסה בבלוק `try/except`.
7. בבלוק ה-`except`, תפסו את השגיאה `KeyError` (שמתרחשת כשמנסים לגשת למפתח שלא קיים במילון).
8. כאשר נתפסת שגיאה, הדפיסו הודעה כגון: "שגיאה בעיבוד ליד: [שם הליד]. שדה חסר.", והמשיכו לליד הבא.

### תוכן הקובץ `leads.json`:
```json
[
    {
        "name": "ישראל ישראלי",
        "company": "טבע תעשיות פרמצבטיות",
        "email": "israel@teva.co.il"
    },
    {
        "name": "עמית חדד",
        "email": "amit@mobileye.com"
    },
    {
        "name": "נועה כספי",
        "company": "בזק",
        "email": "noa.k@bezeq.co.il"
    }
]
```

### קוד מוצא (`process_leads_safely.py`):
```python
# process_leads_safely.py
import json

def process_leads(filename):
    """Reads leads from a JSON file and processes them safely."""
    try:
        with open(filename, 'r', encoding='utf-8') as f:
            leads = json.load(f)
    except FileNotFoundError:
        print(f"Error: File '{filename}' not found.")
        return

    print("מתחיל עיבוד לידים...")
    for lead in leads:
        # כאן יש סיכוי לשגיאה. עטפו את הבלוק הבא ב-try/except
        # -----------------
        name = lead["name"]
        company = lead["company"]
        print(f"מעבד את {name} מ-{company}")
        # -----------------

    print("עיבוד לידים הושלם.")

# --- Main execution ---
process_leads('leads.json')
```

---

## תרגיל 6: אוטומציה מתקדמת - סיווג לידים עם Claude (פרויקט קטן - 40 דקות)
### הנחיות:
נשלב את כל מה שלמדנו עד כה: קריאת נתונים מקובץ, לולאות, קריאות API וטיפול ב-JSON כדי לבנות אוטומציה עסקית שימושית.

1. השתמשו באותו קובץ `leads.json` מהתרגיל הקודם (תקנו אותו כך שלכל הלידים יהיה שדה `company`).
2. צרו קובץ `classify_leads.py`.
3. קראו את הלידים מהקובץ.
4. צרו רשימה ריקה חדשה בשם `classified_leads`.
5. עברו בלולאה על כל ליד. בכל איטרציה:
   א. בנו פרומפט ל-Claude. הפרומפט יכיל את שם החברה של הליד וישאל: "בהתבסס על שם החברה '[שם החברה]', מהו תחום הפעילות המרכזי שלה? (לדוגמה: פיננסים, קמעונאות, סייבר, פארמה)".
   ב. קראו ל-Claude וקבלו את התשובה.
   ג. הוסיפו מפתח חדש לכל מילון של ליד בשם `industry` עם הערך שחזר מ-Claude.
   ד. הוסיפו את הליד המעושר לרשימה `classified_leads`.
6. לאחר סיום הלולאה, שמרו את הרשימה `classified_leads` לקובץ חדש בשם `classified_leads.json`. השתמשו ב-`json.dump` עם `ensure_ascii=False` ו-`indent=4` כדי שהקובץ יהיה קריא בעברית.

### שאלות למחשבה:
- איך הייתם משפרים את הפרומפט כדי לקבל תשובות מדויקות ועקביות יותר מ-Claude?
- מהם הסיכונים בתהליך אוטומטי כזה? (למשל, עלויות API, תשובות לא מדויקות).

---

## תרגיל 7: שליחת עדכון במייל (אינטגרציה - 30 דקות)
### הנחיות:
אוטומציות רבות מסתיימות בשליחת התראה או סיכום. נשתמש בספריית `smtplib` המובנית של פייתון כדי לשלוח מייל סיכום פשוט.

**הערה:** תרגיל זה דורש הגדרות אישיות בחשבון המייל שלכם (למשל, יצירת "סיסמת אפליקציה" ב-Gmail). אם אין לכם אפשרות לעשות זאת, התמקדו בכתיבת הקוד עצמו.

1.  צרו קובץ `send_report.py`.
2.  הוסיפו את פרטי ה-SMTP שלכם (כתובת שרת, פורט, אימייל וסיסמה) לקובץ `.env`.
3.  כתבו פונקציה `send_summary_email(processed_count)` שמקבלת מספר לידים שסווגו.
4.  בתוך הפונקציה, השתמשו בספריית `smtplib` כדי להתחבר לשרת ה-SMTP שלכם (למשל, `smtp.gmail.com` עבור ג'ימייל).
5.  צרו הודעת מייל פשוטה המציינת את מספר הלידים שעובדו.
6.  שלחו את המייל לנמען שתבחרו (יכול להיות אתם עצמכם).
7.  בסוף הסקריפט שלכם מתרגיל 6 (`classify_leads.py`), קראו לפונקציה החדשה שיצרתם כדי לשלוח מייל סיכום לאחר שהתהליך הסתיים.

### קוד מוצא:
```python
# send_report.py
import smtplib
import os
from email.mime.text import MIMEText
from email.header import Header
from dotenv import load_dotenv

load_dotenv()

# Load credentials from .env
SMTP_SERVER = os.getenv("SMTP_SERVER") # e.g., "smtp.gmail.com"
SMTP_PORT = int(os.getenv("SMTP_PORT", 587))
EMAIL_SENDER = os.getenv("EMAIL_SENDER")
EMAIL_PASSWORD = os.getenv("EMAIL_PASSWORD") # App password for Gmail

def send_summary_email(processed_count, recipient_email):
    """Sends a summary email about the lead classification process."""
    
    subject = "סיכום תהליך סיווג לידים"
    body = f"הושלם בהצלחה סיווג של {processed_count} לידים."

    msg = MIMEText(body, 'plain', 'utf-8')
    msg['Subject'] = Header(subject, 'utf-8')
    msg['From'] = EMAIL_SENDER
    msg['To'] = recipient_email

    try:
        print(f"מתחבר לשרת {SMTP_SERVER}...")
        with smtplib.SMTP(SMTP_SERVER, SMTP_PORT) as server:
            server.starttls()  # Secure the connection
            server.login(EMAIL_SENDER, EMAIL_PASSWORD)
            server.sendmail(EMAIL_SENDER, [recipient_email], msg.as_string())
        print(f"מייל סיכום נשלח בהצלחה אל {recipient_email}")
    except Exception as e:
        print(f"שגיאה בשליחת המייל: {e}")

# דוגמת שימוש (ניתן לשלב עם סקריפט אחר)
if __name__ == "__main__":
    send_summary_email(processed_count=50, recipient_email="your.email@example.com")
```

---

## תרגיל 8: משימת בית
### הנחיות:
במשימה זו תבנו כלי אוטומטי שלם לאיתור ופנייה ראשונית לשותפים עסקיים פוטנציאליים. המשימה משלבת את כל הנושאים שנלמדו בשיעור.

**הסיפור:** אתם עובדים בחברת פינטק ישראלית בשם "FinSecure" המפתחת פתרונות אבטחת מידע לבנקים ומוסדות פיננסיים. עליכם לבנות סקריפט ש:

1.  **קורא רשימת חברות:** קורא רשימת חברות פוטנציאליות מקובץ `prospects.json`. הקובץ יכיל רשימת חברות, חלקן בנקים וחלקן לא (לדוגמה: "בנק הפועלים", "שופרסל", "מזרחי טפחות", "אינטל", "בנק דיסקונט").
2.  **מסנן עם AI:** עבור כל חברה ברשימה, קורא ל-Claude API ושואל האם החברה היא "בנק או מוסד פיננסי". יש לבקש מ-Claude להחזיר תשובה בפורמט JSON בלבד, למשל: `{"is_financial": true, "reason": "זוהה כבנק מסחרי מוביל בישראל."}`.
3.  **ממיין את התוצאות:**
    *   אם `is_financial` הוא `true`, שמרו את שם החברה ואת הסיבה (`reason`) בקובץ `financial_targets.json`.
    *   אם `is_financial` הוא `false`, שמרו את שם החברה בקובץ `irrelevant_companies.json`.
4.  **מייצר פנייה אישית:** עבור כל חברה שסווגה כמתאימה (ב-`financial_targets.json`), בצעו קריאת API נוספת ל-Claude כדי לייצר טקסט של 2-3 משפטים לפנייה ראשונית בלינקדאין. הפרומפט צריך לציין שהפנייה היא מטעם חברת "FinSecure" וצריכה להיות מותאמת אישית לשם הבנק הספציפי. שמרו את התוצאות בקובץ `outreach_messages.json`.
5.  **שולח סיכום:** בסיום התהליך כולו, שלחו לעצמכם מייל סיכום שמציין:
    *   כמה חברות נסרקו בסך הכל.
    *   כמה מטרות פיננסיות זוהו.
    *   כמה חברות נפסלו.

**הגישו עד השיעור הבא את קוד הפייתון שלכם ואת קבצי ה-JSON שנוצרו.**

---

## קריטריונים להערכה
| קריטריון | ציון |
|---|---|
| קוד נקי, מאורגן בפונקציות וקריא | 25% |
| שימוש נכון בסביבה וירטואלית ומשתני סביבה (`.env`) | 15% |
| אינטגרציה תקינה עם Claude API (פרומפטים ברורים, עיבוד תשובות) | 30% |
| טיפול בקבצי JSON (קריאה וכתיבה) | 15% |
| שליחת מייל סיכום תקינה | 10% |
| תיעוד והערות בקוד | 5% |

</div>