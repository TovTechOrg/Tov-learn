# Per-machine Claude Code settings (paths, permissions, local overrides)
.claude/settings.local.json

# Learner data lives outside the repo (~/skill-tutor-tutorials); never commit a local copy
skill-tutor-tutorials/settings.json

# OS / editor cruft
Thumbs.db
.DS_Store
.idea/

# secrets
.env
client_secret_*.json

# user export data
*.zip

# dependency / build artifacts inside exercise projects
node_modules/
.next/
__pycache__/
venv/
.venv/
